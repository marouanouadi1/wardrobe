/** Rotta sottile: la sostanza vive in `src/dev/Valutazioni.tsx`, fuori
 * dall'albero delle schermate — è un toolchain interno, non l'app spedita. */
export { default } from '../../src/dev/Valutazioni'
