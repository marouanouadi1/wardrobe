/** Rotta sottile: la sostanza vive in `src/dev/Playground.tsx`, fuori
 * dall'albero delle schermate — è un toolchain interno, non l'app spedita. */
export { default } from '../../src/dev/Playground'
